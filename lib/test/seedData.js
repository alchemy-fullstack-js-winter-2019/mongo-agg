
import { isMainThread } from "worker_threads";
import { create } from "domain";
const chance = new Chance();

var chance = new Chance(
  [...Array(100)].map => Tweet.create({handle: chance.pickone(['ryan', 'another', 'a third'
])
    , text:''}))
});
});